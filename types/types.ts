export type LoginType = {
    email: string,
    password: string
}

export type SignupType = {
    email: string,
    fullname: string,
    password: string,
    confirmpassword: string,
    dob: string,
}